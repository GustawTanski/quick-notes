export default class NoteModel {
    constructor(node, title, content, color) {
        this.node = node;
        this.title = title;
        this.content = content;
        this.color = color;
    }
}